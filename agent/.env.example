ARC_TESTNET_RPC_URL=https://rpc.testnet.arc.network
# World Chain RPC for the AgentBook lookupHuman read. Identity lives on World
# Chain (the agent is registered there); the agentkit verifier defaults to World
# Chain, so this is only needed to override the public default with a dedicated
# endpoint (Alchemy/etc.) for rate limits.
WORLDCHAIN_RPC_URL=
# Base Sepolia RPC, used by getAgentWallet() for Base-side calls (84532). Not the
# identity chain; AgentBook registration and lookupHuman are on World Chain.
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
# Dynamic MPC server wallet credentials (Saturday workstream)
# Environment ID is public (same value web exposes via NEXT_PUBLIC_).
DYNAMIC_ENVIRONMENT_ID=
# API token is SECRET. Generate at app.dynamic.xyz -> Developers -> API Tokens.
# Starts with dyn_. Never commit the real value; it belongs only in agent/.env.
# The Node SDK authenticates this via authenticateApiToken(); some docs/briefs
# call it DYNAMIC_AUTH_TOKEN. Either name is accepted, this one is preferred.
DYNAMIC_API_TOKEN=
# Backup password that encrypts the MPC server key share stored at Dynamic.
# SECRET and DURABLE: required to create a recoverable wallet and to sign with it
# later. Must never change once a wallet exists, and must be backed up securely.
# If it is lost the agent wallet can never sign again.
AGENT_WALLET_PASSWORD=
